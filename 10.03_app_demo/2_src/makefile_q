# makefile_q: QBUS build of "demo".
# Only the UNIBUS/QBUS deltas live here; everything shared is in
# makefile.common. Add new device objects THERE, unless the device exists on
# one bus only.

PLATFORM_SUFFIX = _q
PLATFORM_CCDEFS = -DQBUS
PRU1_CODE_BUSNAME = qbus

# no QBUS-only devices at present
PLATFORM_OBJECTS =

include makefile.common
